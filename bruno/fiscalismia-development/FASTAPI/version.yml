info:
  name: version
  type: http
  seq: 2

http:
  method: GET
  url: http://{{FASTAPI_ENDPOINT}}/version
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: etl aldi prospekt [manual]
  type: http
  seq: 12

http:
  method: POST
  url: "{{PROTOCOL}}://{{FASTAPI_ENDPOINT}}/fastapi/fiscalismia/rest/cdp/scrape/etl/aldi/01d66abe-90b8-4631-9719-56f05c0a9c2c"
  auth:
    type: bearer
    token: "{{JWT_SESSION_TOKEN}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const sessionId = res.body?.session_id;
            if (sessionId) {
              bru.setVar("SESSION_ID", sessionId);
            }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

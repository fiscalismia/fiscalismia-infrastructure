info:
  name: write mouseinput ws
  type: websocket
  seq: 6

websocket:
  url: "{{WS_PROTOCOL}}://{{FASTAPI_ENDPOINT}}/fastapi/ws/session/{{SESSION_ID}}"
  message:
    type: json
    data: |-
      {
        "type": "custom_mouse_click",
        "x": 12,
        "y": 25,
        "button": "left"
      }
  auth: inherit

settings:
  timeout: 5000
  keepAliveInterval: 0

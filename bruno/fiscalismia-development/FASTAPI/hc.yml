info:
  name: hc
  type: http
  seq: 1

http:
  method: GET
  url: "{{PROTOCOL}}://{{FASTAPI_ENDPOINT}}/fastapi/hc"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

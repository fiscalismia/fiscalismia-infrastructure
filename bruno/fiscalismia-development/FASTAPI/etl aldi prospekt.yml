info:
  name: etl aldi prospekt
  type: http
  seq: 11

http:
  method: POST
  url: "{{PROTOCOL}}://{{FASTAPI_ENDPOINT}}/fastapi/fiscalismia/rest/cdp/scrape/etl/aldi/{{SESSION_ID}}"
  auth:
    type: bearer
    token: "{{JWT_SESSION_TOKEN}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const sessionId = res.body?.session_id;
            if (sessionId) {
              bru.setVar("SESSION_ID", sessionId);
            }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

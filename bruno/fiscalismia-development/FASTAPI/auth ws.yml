info:
  name: auth ws
  type: websocket
  seq: 4

websocket:
  url: "{{WS_PROTOCOL}}://{{FASTAPI_ENDPOINT}}/fastapi/ws/session/{{SESSION_ID}}"
  message:
    type: text
    data: "{{JWT_SESSION_TOKEN}}"
  auth: inherit

settings:
  timeout: 5000
  keepAliveInterval: 0

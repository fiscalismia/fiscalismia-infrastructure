info:
  name: read ws
  type: websocket
  seq: 5

websocket:
  url: "{{WS_PROTOCOL}}://{{FASTAPI_ENDPOINT}}/fastapi/fiscalismia/ws/session/{{SESSION_ID}}"
  auth: inherit

settings:
  timeout: 5000
  keepAliveInterval: 0

info:
  name: test stream
  type: http
  seq: 3

http:
  method: GET
  url: "{{FASTAPI_ENDPOINT}}/api/fiscalismia/stream/test_cdp_websocket"
  auth:
    type: bearer
    token: "{{JWT_SESSION_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

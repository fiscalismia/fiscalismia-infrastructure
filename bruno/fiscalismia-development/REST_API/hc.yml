info:
  name: hc
  type: http
  seq: 2

http:
  method: GET
  url: "{{PROTOCOL}}://{{REST_API_ENDPOINT}}/api/fiscalismia/hc"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

my ssl certs now seem to work with this new logic applied in my pipeline
```

    - name: Prepare DEMO Workspace
      if: inputs.deploy_demo == true
      env:
        REMOTE_DEMO_DIR: /usr/local/etc/fiscalismia-demo
      run: |
        echo "REMOTE_DEMO_DIR=$REMOTE_DEMO_DIR" >> $GITHUB_ENV
        echo "FRONTEND_DIR=${GITHUB_WORKSPACE}/fiscalismia-frontend" >> $GITHUB_ENV
        echo "BACKEND_DIR=${GITHUB_WORKSPACE}/fiscalismia-backend" >> $GITHUB_ENV
        echo "INFRA_DIR=${GITHUB_WORKSPACE}/fiscalismia-infrastructure" >> $GITHUB_ENV

    - name: Initialize DEMO Instance
      if: inputs.deploy_demo == true
      run: |
        ssh demo << EOF
        mkdir -p ${REMOTE_DEMO_DIR}
        mkdir -p ${REMOTE_DEMO_DIR}/database
        mkdir -p ${REMOTE_DEMO_DIR}/frontend
        mkdir -p ${REMOTE_DEMO_DIR}/backend
        mkdir -p ${REMOTE_DEMO_DIR}/certs

        cp /etc/letsencrypt/live/demo.fiscalismia.com/fullchain.pem ${REMOTE_DEMO_DIR}/certs/
        cp /etc/letsencrypt/live/demo.fiscalismia.com/privkey.pem ${REMOTE_DEMO_DIR}/certs/
        chown -R 101:101 ${REMOTE_DEMO_DIR}/certs/
        chmod -R 400 ${REMOTE_DEMO_DIR}/certs/*.pem
        EOF
```
and docker compose
```

  fiscalismia-frontend:
    image: ghcr.io/fiscalismia/fiscalismia-frontend-demo:latest
    container_name: fiscalismia-frontend
    pull_policy: always
    restart: unless-stopped
    volumes:
      # The ':z' flag is required on SELinux-enabled systems (like Fedora/CentOS)
      # to relabel the host directory. This allows the container to access the
      # mounted volume by applying a shared SELinux label.
      - /usr/local/etc/fiscalismia-demo/certs/fullchain.pem:/etc/nginx/certs/fullchain.pem:ro,z
      - /usr/local/etc/fiscalismia-demo/certs/privkey.pem:/etc/nginx/certs/privkey.pem:ro,z
    ports:
      - 443:443
    # Container Resource Usage
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.4'
        reservations:
          memory: 256M
          cpus: '0.2'
    ### SECURITY HARDENING (minimal required permissions)
    tmpfs:
      - /tmp:noexec,nosuid,size=256m
      - /var/cache/nginx:noexec,nosuid,size=128m
      - /var/run:noexec,nosuid,size=64m
    cap_drop:
      - ALL
    cap_add:
      # if binding to ports < 1024 also requires the following setting on the host:
      # echo "net.ipv4.ip_unprivileged_port_start=80" | sudo tee /etc/sysctl.d/nonroot_can_bind_to_low_ports.conf
      - NET_BIND_SERVICE
      # - CAP_DAC_OVERRIDE # Bypass file read, write, and execute permission checks.
    security_opt:
      - no-new-privileges:true
```

but now i am getting permission denied on the cache directory even though the tmpfs is set in docker compose
it works when adding CAP_DAC_OVERRIDE but i do not want to bypass these security best practices

error:
fiscalismia-frontend  | 2025/12/16 18:26:14 [emerg] 1#1: mkdir() "/var/cache/nginx/client_temp" failed (13: Permission denied)
fiscalismia-frontend  | nginx: [emerg] mkdir() "/var/cache/nginx/client_temp" failed (13: Permission denied)
fiscalismia-frontend  | nginx: [emerg] mkdir() "/var/cache/nginx/client_temp" failed (13: Permission denied)
fiscalismia-frontend  | nginx: [emerg] mkdir() "/var/cache/nginx/client_temp" failed (13: Permission denied)

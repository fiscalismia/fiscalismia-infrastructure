### High Prio
- Centralize a resource list of versions, binaries and dependencies to update during security passes, currently known:
+ think about updating binaries that are hardcoded to a specific version, currently known: AWS_SIGNING_HELPER_VERSION="1.8.0"
+ requirements.txt in Lambda Layers
+ all github actions workflow versions
+ terraform providers "~>" defining max version
+ RUNTIME_ENV + PYTHON_V + DOCKER_IMG in lambda github actions pipelines
+ 

### Medium Prio
- Unify hc endpoints across containers, baking in commit and build-time and environment if possible, following example in go build
- move hcloud certbot aws setup to use x.509 end-entity cert with specific profile in config instead of aws credentials file
- remove port 80 from hcloud public firewall https rule, from all nftables of demo instance as well and check for remaining files

### Low Prio
- rethink monitoring integration, currently another monitoring instance is architected for prod, with resources present in terraform (dns, hetzner vps)
- - perhaps we can switch this to the go binary and a telegram bot instead